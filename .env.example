# NestJS Backend Environment Variables

# Server Configuration (3006 to avoid MCP port conflicts)
PORT=3006
HOST=0.0.0.0
NODE_ENV=development

# Database Configuration (shared with frontend)
DATABASE_URL="postgresql://username:password@localhost:5432/marketsage"

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-here"
JWT_EXPIRES_IN="24h"

# Redis Configuration (for session management)
REDIS_URL="redis://localhost:6379"
REDIS_HOST="localhost"
REDIS_PORT=6379

# Cors Configuration
CORS_ORIGIN="http://localhost:3000,https://*.vercel.app,https://*.railway.app"

# Logging
LOG_LEVEL="debug"